-- Safe version that works on ALL MySQL versions

-- likes
CREATE INDEX idx_likes_user_id     ON likes    (user_id);
CREATE INDEX idx_likes_post_id     ON likes    (post_id);

-- comments
CREATE INDEX idx_comments_user_id  ON comments (user_id);
CREATE INDEX idx_comments_post_id  ON comments (post_id);

-- reposts
CREATE INDEX idx_reposts_user_id   ON reposts  (user_id);
CREATE INDEX idx_reposts_orig_post ON reposts  (original_post_id);

-- posts
CREATE INDEX idx_posts_user_id     ON posts    (user_id);

CREATE INDEX idx_follows_follower  ON follows  (follower_id);dkbs